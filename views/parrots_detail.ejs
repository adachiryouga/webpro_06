<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= parrot.name %></title>
  <style>
    /* 自然を感じるデザイン */
    body {
      font-family: sans-serif;
      background-color: #f1f8e9; /* 薄い緑色の背景 */
    }
    h2 {
      color: #2e7d32;
      border-bottom: 2px solid #2e7d32;
    }
    table {
      border-collapse: collapse;
      width: 100%;
      max-width: 600px;
      margin-top: 20px;
      background-color: #fff;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 10px;
    }
    th {
      background-color: #a5d6a7; /* 明るい緑 */
      width: 30%;
      text-align: left;
    }
  </style>
</head>
<body>
  <h2><%= parrot.name %> の詳細</h2>
  
  <div>
    <% if(parrot.image) { %>
      <img src="/public/images/<%= parrot.image %>" alt="<%= parrot.name %>" width="300" style="border: 5px solid #fff; box-shadow: 2px 2px 5px rgba(0,0,0,0.2);">
    <% } else { %>
      <p>画像はありません</p>
    <% } %>
  </div>

  <table>
    <tr><th>項目</th><th>データ</th></tr>
    <tr><th>名前</th><td><%= parrot.name %></td></tr>
    <tr><th>色</th><td><%= parrot.color %></td></tr>
    <tr><th>サイズ</th><td><%= parrot.size %></td></tr>
    <tr><th>寿命</th><td><%= parrot.lifespan %></td></tr>
    <tr><th>特徴</th><td><%= parrot.feature %></td></tr>
    <tr><th>説明</th><td><%= parrot.description %></td></tr>
  </table>

  <div style="margin-top: 20px;">
    <a href="/parrots">一覧に戻る</a>
  </div>
</body>
</html>